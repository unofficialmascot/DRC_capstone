import { ApplicationShell } from "./ApplicationShell";

export default function PreTalkApplication() {
  return (
    <ApplicationShell
      title="Pre-talk Application"
      description="Start the pre-talk application workflow."
    >
      <div className="text-center text-muted-foreground">
        Form content will be added here.
      </div>
    </ApplicationShell>
  );
}
