import { ApplicationShell } from "./ApplicationShell";

export default function ChangeSupervisor() {
  return (
    <ApplicationShell
      title="Change of Supervisor"
      description="Submit a request to change or add a supervisor."
    >
      <div className="text-center text-muted-foreground">
        Form content will be added here.
      </div>
    </ApplicationShell>
  );
}
